import SwiftUI

struct ScanView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("扫描识别")
                    .font(.title)
            }
            .navigationTitle("扫描")
        }
    }
} 